name: eyecaresystem
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you plan to publish

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust SDK range to be compatible with latest Flutter

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8 # Already present, consider updating to latest if you want
  google_fonts: ^6.1.0    # Already present, consider updating to latest if you want
  http: ^1.2.1            # Current version is 1.3.0, update to latest stable if needed
  shared_preferences: ^2.2.3 # Current version is 2.2.2, update to latest stable if needed
  provider: ^6.0.5        # ADD THIS LINE

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0   # Current version is 5.0.0, consider using the latest `flutter_lints`

flutter:
  uses-material-design: true